#pragma config(Sensor, in1,    liftPot,        sensorPotentiometer)
#pragma config(Sensor, dgtl1,  hippo,          sensorDigitalOut)
#pragma config(Sensor, dgtl2,  hungryHungry,   sensorDigitalOut)
#pragma config(Motor,  port1,           rbd,           tmotorVex393_HBridge, openLoop, reversed)
#pragma config(Motor,  port2,           lbd,           tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port3,           lfd,           tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port4,           rfd,           tmotorVex393_MC29, openLoop, reversed)
#pragma config(Motor,  port5,           lift1,         tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port6,           lift2,         tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port7,           lift3,         tmotorVex393_MC29, openLoop, reversed)
#pragma config(Motor,  port8,           lift4,         tmotorVex393_MC29, openLoop, reversed)
#pragma config(Motor,  port9,           elevator1,     tmotorVex393_MC29, openLoop)
#pragma config(Motor,  port10,          elevator2,     tmotorVex393_HBridge, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

#pragma platform(VEX2)
#pragma competitionControl(Competition)
#include "Vex_Competition_Includes.c"

#include "buttonTracker.c"
#include "parallelDrive.c"
#include "motorGroup.c"

//buttons
#define toggleClawBtn Btn6U //claw
#define liftUpBtn Btn5U //lift
#define liftDownBtn Btn5D
#define elevatorUpBtn Btn7U //elevator
#define elevatorDownBtn Btn7D

//positions
#define liftMiddle 1010

//constants
#define stillSpeedMagnitude 10

parallel_drive drive;
motorGroup lift;
motorGroup elevator;

void pre_auton() {
	bStopTasksBetweenModes = true;

	initializeDrive(drive);
  setDriveMotors(drive, 4, lfd, lbd, rfd, rbd);

  initializeGroup(lift, 4, lift1, lift2, lift3, lift4);
  configureButtonInput(lift, liftUpBtn, liftDownBtn, stillSpeedMagnitude);
  addSensor(lift, liftPot, true);

  initializeGroup(elevator, 2, elevator1, elevator2);
  configureButtonInput(elevator, elevatorUpBtn, elevatorDownBtn);
}

task autonomous() {
  AutonomousCodePlaceholderForTesting();
}

task usercontrol() {
	while (true) {
  	driveRuntime(drive);

  	takeInput(elevator);

  	lift.stillSpeed = stillSpeedMagnitude * (potentiometerVal(lift)>liftMiddle ? 1 : -1);
  	takeInput(lift);

		if (newlyPressed(toggleClawBtn)) {
			SensorValue[hungryHungry] = 1 - SensorValue[hungryHungry];
		}
  }
}
